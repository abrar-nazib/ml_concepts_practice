# Neural networks

MLPs from scratch — manual backprop, then a tiny autograd engine, then PyTorch.
Optimizers (SGD, momentum, Adam), regularization (dropout, weight decay,
batchnorm), and initialization.
